import { LoginPage } from '../../src/presentation/pages/LoginPage';

export const metadata = {
  title: 'Login — IA Jurídica',
  description: 'Inicia sesión en IA Jurídica',
};

export default function LoginRoute() {
  return <LoginPage />;
}
