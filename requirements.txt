# Core Dependencies
fastapi>=0.112.0,<0.113.0
uvicorn[standard]>=0.25.0,<0.26.0
pydantic>=2.10.0
pydantic-settings>=2.0.0
pydantic-ai>=0.0.14,<0.5.0

# LLM Providers
cohere>=5.13.11,<5.14  # Pinned: 5.14+ renames classes that pydantic-ai 0.4.x imports

# Document Processing
docling==2.5.2  # Versión fija para estabilidad
pypdf2>=3.0.1
python-multipart>=0.0.9,<0.0.10

# OpenCV - CONTROL EXPLÍCITO para evitar conflictos
# (dependencia transitiva de docling → easyocr → opencv)
opencv-python-headless>=4.6,<4.10  # Compatible con numpy 1.26

# RAG and Knowledge Graphs
lightrag-hku>=1.4.9

# Translation (opcional, para NLLB)
transformers>=4.46.0
sentence-transformers>=3.3.0

# Evaluation
deepeval>=1.3.2

# Database
sqlalchemy>=2.0.36

# PDF Generation
reportlab>=4.0

# Numeric Libraries - VERSIÓN COORDINADA
numpy>=1.26,<2.0  # Requerido por: opencv, docling, embeddings

# Security & Rate Limiting
slowapi>=0.1.9

# Utilities
python-dotenv>=1.0.0
loguru>=0.7.0
tqdm>=4.67.0
rich>=13.9.0
click>=8.1.0

# Development
pytest>=8.3.0
pytest-asyncio>=0.23.0
black>=24.10.0
flake8>=7.1.0